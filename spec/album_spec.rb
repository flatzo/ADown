require 'spec_helper'

describe ADown::Album do
  
  before :each do 
    @album = ADown::Album.new(1)
  end

  describe "#download" do
    it "has at least " do 
      #lambda (@album.download).should raise_exception ArgumentError
    end
  end

    
end